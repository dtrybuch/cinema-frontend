import { SearchRepertoirePipe } from './search-repertoire.pipe';

describe('SearchRepertoirePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRepertoirePipe();
    expect(pipe).toBeTruthy();
  });
});
